# hard code example to see if code runs

ns=42
nb=2
no=11646

folder_path=/home/x_gaedkelb/b_INLA/data/ns${ns}

echo "./example ${ns} ${nb} ${no} ${folder_path}"
./example ${ns} ${nb} ${no} ${folder_path}
